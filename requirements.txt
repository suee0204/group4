# Smart Fire Alert System - Python Dependencies
# Core Flask Web Application Dependencies
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7

# Database Dependencies
PyMySQL==1.1.0
SQLAlchemy==2.0.21

# Camera and Image Processing (Raspberry Pi)
picamera2==0.3.12
libcamera==0.0.5
numpy==1.24.3
Pillow==10.0.1

# HTTP Requests for Telegram Bot
requests==2.31.0
urllib3==2.0.5
certifi==2023.7.22
charset-normalizer==3.3.0
idna==3.4

# Hardware Abstraction Layer (HAL) - Custom modules
# Note: hal modules are from the git submodule and should be available in the container

# System and Threading (built-in Python modules)
# time, threading, queue are built-in

# Development and Testing
pytest==7.4.2
pytest-flask==1.2.0

# Optional: For better JSON handling and debugging
python-json-logger==2.0.7

# Raspberry Pi GPIO and Hardware (if running on actual Pi hardware)
# Note: These may need to be installed separately on actual Pi hardware
# RPi.GPIO==0.7.1
# gpiozero==1.6.2

# For production WSGI server
gunicorn==21.2.0

# Environment variables management
python-dotenv==1.0.0